{
  "name": "med-landing-dev",
  "version": "1.6.0",
  "description": "WordPress custom theme for nephrologist - Xalapa & Boca del Rio",
  "scripts": {
    "dev": "npx @tailwindcss/cli -i ./assets/css/src/main.css -o ./style.css --watch",
    "build": "npx @tailwindcss/cli -i ./assets/css/src/main.css -o ./assets/css/tailwind-output.css --minify && node build-css.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0"
  }
}
